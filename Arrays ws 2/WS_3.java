import java.util.*;
public class WS_3 {
    public void main() {
        Scanner sc = new Scanner(System.in);
        int y[];
        int t=0;
        int l
        for(int c=0; c<c+1; c++){
        System.out.print("Enter a year, or enter negative no. to exit:" );
        t=sc.nextInt();
        if(t<0)
        break;
        else if(t%4==0)
        lyc
    }
}